<h1><%= @wiki.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @wiki.body %></p>
  </div>
  <div class="col-md-4">
    <%= link_to "Edit Wiki Post", edit_wiki_path(@wiki), class: 'btn btn-success' %>
    <%= link_to "Delete Wiki Post", @wiki, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this Wiki posting?'} %>
  </div>
</div>